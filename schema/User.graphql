enum Status{
    Active
    Inactive
}

type User {
    id: ID!
    name: String!
    email: String!
    age: Int,
    wage: Float,
    vip: Boolean,
    profile: Profile!
    status: Status!
}

type Profile {
    id: ID!
    name: String!
} 